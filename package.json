{
  "name": "luxe-store",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "ecommerce-frontend",
    "ecommerce-backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=ecommerce-frontend",
    "start": "npm start --workspace=ecommerce-backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
