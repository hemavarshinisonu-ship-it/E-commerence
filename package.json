{
  "name": "luxe-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ecommerce-frontend && npm install && npm run build",
    "start": "cd ecommerce-backend && npm start"
  }
}
